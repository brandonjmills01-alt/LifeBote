[build]
builder = "nixpacks"
buildCommand = "pip3 install -r backend/requirements.txt && cd frontend && npm install && npm run build"

[deploy]
startCommand = "cd backend && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"