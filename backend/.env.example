# Copy this file to .env — never commit .env to git

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-with-a-strong-random-string

# From platform.openai.com — leave as placeholder to use rule-based fallback
OPENAI_API_KEY=sk-placeholder

# From rapidapi.com/letscrape-6bfcfa — leave as placeholder to use mock jobs
JSEARCH_API_KEY=placeholder
